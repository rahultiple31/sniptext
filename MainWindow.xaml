<Window x:Class="SnipText.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SnipText"
        Height="720"
        Width="1120"
        MinHeight="580"
        MinWidth="880"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FB">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CAD3E1"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#162033"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#176B87"/>
            <Setter Property="BorderBrush" Value="#176B87"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#DDE4EF" BorderThickness="0,0,0,1">
            <DockPanel LastChildFill="True" Margin="20,14">
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <TextBlock Text="SnipText" FontSize="22" FontWeight="SemiBold" Foreground="#122033" Margin="0,0,20,0" VerticalAlignment="Center"/>
                    <Button x:Name="NewSnipButton" Style="{StaticResource PrimaryButton}" Content="New snip" Click="NewSnipButton_Click"/>
                    <Button x:Name="OpenImageButton" Content="Open image" Click="OpenImageButton_Click"/>
                    <Button x:Name="ReadTextButton" Content="Read text" Click="ReadTextButton_Click"/>
                    <Button x:Name="SaveImageButton" Content="Save image" Click="SaveImageButton_Click"/>
                    <Button x:Name="SaveTextButton" Content="Save text" Click="SaveTextButton_Click"/>
                    <Button x:Name="ClearButton" Content="Clear" Click="ClearButton_Click"/>
                </StackPanel>

                <CheckBox x:Name="AutoReadCheckBox"
                          DockPanel.Dock="Right"
                          Content="Auto read"
                          IsChecked="True"
                          VerticalAlignment="Center"
                          Foreground="#34445C"/>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.05*"/>
                <ColumnDefinition Width="0.95*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#FFFFFF" BorderBrush="#DDE4EF" BorderThickness="1" CornerRadius="8" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Capture" FontWeight="SemiBold" FontSize="15" Foreground="#162033" Margin="16,14,16,8"/>
                    <Border Grid.Row="1" Margin="16,0,16,16" Background="#E9EEF5" BorderBrush="#D1D9E6" BorderThickness="1" CornerRadius="6">
                        <Grid>
                            <Image x:Name="PreviewImage" Stretch="Uniform" Margin="12"/>
                            <StackPanel x:Name="EmptyImagePanel" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="No image selected" FontSize="18" FontWeight="SemiBold" Foreground="#536178" HorizontalAlignment="Center"/>
                                <TextBlock Text="Use New snip or Open image" FontSize="13" Foreground="#6F7C91" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="#FFFFFF" BorderBrush="#DDE4EF" BorderThickness="1" CornerRadius="8" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="16,14,16,8">
                        <Button DockPanel.Dock="Right" x:Name="CopyTextButton" Content="Copy" Click="CopyTextButton_Click" Margin="8,0,0,0"/>
                        <TextBlock Text="Extracted text" FontWeight="SemiBold" FontSize="15" Foreground="#162033" VerticalAlignment="Center"/>
                    </DockPanel>
                    <TextBox x:Name="TextResultBox"
                             Grid.Row="1"
                             Margin="16,0,16,16"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="14"
                             BorderBrush="#D1D9E6"
                             Background="#FBFCFE"/>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#DDE4EF" BorderThickness="0,1,0,0">
            <TextBlock x:Name="StatusText" Text="Ready" Foreground="#536178" Margin="20,9"/>
        </Border>
    </Grid>
</Window>
